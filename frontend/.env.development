# Express server (OSCWebMixer index.js). Override if config.json uses a different port.
VITE_BACKEND_TARGET=http://127.0.0.1:80
# WebSocket target in dev — same host/port as the backend (cannot proxy root-path WS via Vite).
VITE_WS_ORIGIN=ws://127.0.0.1:80
